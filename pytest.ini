[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
# --junitxml : génère un rapport JUnit XML parsable par les pipelines CI/CD (GitHub Actions, Jenkins, SonarQube)
addopts = --junitxml=tests/results/results.xml -v
log_cli = true
log_cli_level = INFO
